<UserControl x:Class="spendsmart.Views.ReportPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <UserControl.Resources>
        <Style x:Key="ReportTabStyle" TargetType="RadioButton">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#D8D8D8" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                            <Border x:Name="Underline"
                                    Height="3"
                                    Background="Transparent"
                                    VerticalAlignment="Bottom" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="#419CDF" />
                                <Setter TargetName="Underline" Property="Background" Value="#419CDF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="96" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,8,20,0">
            <TextBlock Text="22:02"
                       FontSize="22"
                       FontWeight="Bold"
                       VerticalAlignment="Top" />
            <TextBlock Text="▣"
                       FontSize="32"
                       Foreground="#419CDF"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Bottom" />
            <Border Width="250"
                    Height="38"
                    Background="#E8E8EA"
                    CornerRadius="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Border Margin="2"
                            Background="#419CDF"
                            CornerRadius="10">
                        <TextBlock Text="Hàng tháng"
                                   Foreground="White"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Grid.Column="1"
                               Text="Hàng năm"
                               Foreground="#419CDF"
                               FontSize="18"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False">
            <StackPanel>
                <Border BorderBrush="#DADADA"
                        BorderThickness="0,1,0,0"
                        Padding="18,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="50" />
                        </Grid.ColumnDefinitions>
                        <Button Content="&lt;"
                                Command="{Binding PreviousMonthCommand}"
                                Width="42"
                                Height="42"
                                BorderThickness="0"
                                Background="Transparent"
                                Foreground="#555"
                                FontSize="28"
                                FontWeight="Bold"
                                HorizontalAlignment="Left" />
                        <Border Grid.Column="1"
                                Height="42"
                                Background="#EEF8FE"
                                CornerRadius="8">
                            <TextBlock Text="{Binding MonthText}"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Foreground="#555"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>
                        <Button Grid.Column="2"
                                Content="&gt;"
                                Command="{Binding NextMonthCommand}"
                                Width="42"
                                Height="42"
                                BorderThickness="0"
                                Background="Transparent"
                                Foreground="#555"
                                FontSize="28"
                                FontWeight="Bold"
                                HorizontalAlignment="Right" />
                    </Grid>
                </Border>

                <Grid Margin="24,12,24,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Border BorderBrush="#DDD"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10,8">
                        <DockPanel>
                            <TextBlock Text="Chi tiêu"
                                       FontSize="16"
                                       Foreground="#666" />
                            <TextBlock Text="{Binding TotalExpenseText}"
                                       FontSize="21"
                                       FontWeight="Bold"
                                       Foreground="#F26B5A"
                                       HorizontalAlignment="Right" />
                        </DockPanel>
                    </Border>
                    <Border Grid.Column="2"
                            BorderBrush="#DDD"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10,8">
                        <DockPanel>
                            <TextBlock Text="Thu nhập"
                                       FontSize="16"
                                       Foreground="#666" />
                            <TextBlock Text="{Binding TotalIncomeText}"
                                       FontSize="21"
                                       FontWeight="Bold"
                                       Foreground="#419CDF"
                                       HorizontalAlignment="Right" />
                        </DockPanel>
                    </Border>
                </Grid>
                <Border Margin="24,0,24,18"
                        BorderBrush="#DDD"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="10,8">
                    <DockPanel>
                        <TextBlock Text="Thu chi"
                                   FontSize="16"
                                   Foreground="#666" />
                        <TextBlock Text="{Binding BalanceText}"
                                   FontSize="23"
                                   FontWeight="Bold"
                                   Foreground="#555"
                                   HorizontalAlignment="Right" />
                    </DockPanel>
                </Border>

                <Grid Margin="24,0,24,10" Height="38">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <RadioButton Content="Chi tiêu"
                                 IsChecked="{Binding IsExpenseSelected, Mode=OneWay}"
                                 Command="{Binding ShowExpenseCommand}"
                                 Style="{StaticResource ReportTabStyle}" />
                    <RadioButton Grid.Column="1"
                                 Content="Thu nhập"
                                 IsChecked="{Binding IsIncomeSelected, Mode=OneWay}"
                                 Command="{Binding ShowIncomeCommand}"
                                 Style="{StaticResource ReportTabStyle}" />
                </Grid>

                <Grid Width="260"
                      Height="230"
                      HorizontalAlignment="Center">
                    <TextBlock Text="{Binding EmptyMessage}"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               FontSize="16"
                               Foreground="#999"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center" />
                    <lvc:PieChart Series="{Binding ChartSeries}"
                                  LegendPosition="Hidden"
                                  TooltipPosition="Hidden"
                                  InitialRotation="-90" />
                </Grid>

                <ItemsControl ItemsSource="{Binding CategoryItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DADADA"
                                    BorderThickness="0,1,0,0"
                                    Padding="18,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="54" />
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="128" />
                                        <ColumnDefinition Width="70" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Icon}"
                                               FontSize="25"
                                               Foreground="{Binding Color}"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding CategoryName}"
                                               FontSize="23"
                                               FontWeight="Bold"
                                               Foreground="#555"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding TotalAmountText}"
                                               FontSize="21"
                                               FontWeight="Bold"
                                               Foreground="#555"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="3"
                                               Text="{Binding PercentageText}"
                                               FontSize="17"
                                               Foreground="#666"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
