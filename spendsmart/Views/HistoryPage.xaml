<UserControl x:Class="spendsmart.Views.HistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="96" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,8,20,0">
            <TextBlock Text="22:01"
                       FontSize="22"
                       FontWeight="Bold"
                       VerticalAlignment="Top" />
            <TextBlock Text="Lịch"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#555"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Bottom" />
            <TextBlock Text="⌕"
                       FontSize="38"
                       Foreground="#419CDF"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Bottom" />
        </Grid>

        <Border Grid.Row="1"
                BorderBrush="#DADADA"
                BorderThickness="0,1,0,1"
                Padding="18,12">
            <Grid>
                <Button Content="‹"
                        Command="{Binding PreviousMonthCommand}"
                        Width="42"
                        Height="42"
                        BorderThickness="0"
                        Background="Transparent"
                        Foreground="#555"
                        FontSize="34"
                        HorizontalAlignment="Left" />
                <Border Width="398"
                        Height="42"
                        Background="#EEF8FE"
                        CornerRadius="8"
                        HorizontalAlignment="Center">
                    <TextBlock Text="{Binding MonthText}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#555"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <Button Content="›"
                        Command="{Binding NextMonthCommand}"
                        Width="42"
                        Height="42"
                        BorderThickness="0"
                        Background="Transparent"
                        Foreground="#555"
                        FontSize="34"
                        HorizontalAlignment="Right" />
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="18,14,18,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Thu nhập"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="#555"
                           HorizontalAlignment="Center" />
                <TextBlock Text="{Binding TotalIncomeText}"
                           FontSize="23"
                           FontWeight="Bold"
                           Foreground="#419CDF"
                           HorizontalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Chi tiêu"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="#555"
                           HorizontalAlignment="Center" />
                <TextBlock Text="{Binding TotalExpenseText}"
                           FontSize="23"
                           FontWeight="Bold"
                           Foreground="#F05A3A"
                           HorizontalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Tổng"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="#555"
                           HorizontalAlignment="Center" />
                <TextBlock Text="{Binding BalanceText}"
                           FontSize="23"
                           FontWeight="Bold"
                           Foreground="#F05A3A"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3">
            <TextBlock Text="{Binding EmptyMessage}"
                       Margin="24,40,24,0"
                       TextAlignment="Center"
                       FontSize="18"
                       Foreground="#999" />

            <ListBox ItemsSource="{Binding Transactions}"
                     BorderThickness="0"
                     Background="White"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DADADA"
                                BorderThickness="0,0,0,1"
                                Padding="18,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="54" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding CategoryIcon}"
                                           FontSize="28"
                                           Foreground="{Binding CategoryColor}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding CategoryName}"
                                               FontSize="23"
                                               FontWeight="Bold"
                                               Foreground="#555"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding DateText}"
                                               Margin="0,2,0,0"
                                               FontSize="14"
                                               Foreground="#777" />
                                    <TextBlock Text="{Binding Note}"
                                               Margin="0,2,0,0"
                                               FontSize="14"
                                               Foreground="#999"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <StackPanel Grid.Column="2"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="{Binding AmountText}"
                                               FontSize="22"
                                               FontWeight="Bold"
                                               Foreground="{Binding AmountColor}"
                                               HorizontalAlignment="Right" />
                                    <TextBlock Text="{Binding TypeText}"
                                               FontSize="13"
                                               Foreground="#999"
                                               HorizontalAlignment="Right" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
